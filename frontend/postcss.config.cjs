module.exports = {
  plugins: {
    // replace any `tailwindcss: {}` entry with:
    "@tailwindcss/postcss": {},
    // you can keep autoprefixer if you need it, but in v4 it may be optional:
    autoprefixer: {},
  },
};
